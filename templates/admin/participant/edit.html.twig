{% extends 'base.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
<div class="uk-card uk-card-default m-4 p-4">
    <div class="uk-width-1-1 uk-text-left">
        <div class="uk-card-title">
            <h3>Edition article</h3>
        </div>

        {{ include('admin/participant/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="uk-card-footer">
            <a href="{{ path('admin_participant_index') }}" class="uk-button yk-button-secondary uk-align-right">Listes</a>

            {{ include('admin/participant/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
